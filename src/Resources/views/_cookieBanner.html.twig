<div class="js-kwc-wrapper {{ bannerClasses.wrapper }}">

    Questo sito utilizza cookie tecnici per i quali non è richiesto consenso.


    <div class="{{ bannerClasses.actionWrapper }}">
        <a href="{{ path('kikwik_cookie_save') }}" class="js-kwc-btn-accept {{ bannerClasses.btnAccept }}">Accetta</a>
{#        <a href="#" class="js-kwc-btn-deny {{ bannerClasses.btnDeny }}">Rifiuta</a>#}
{#        <a href="#" class="js-kwc-btn-choose {{ bannerClasses.btnChoose }}">Scegli</a>#}
        {% if privacy_url %}
            <a href="{{ privacy_url }}" class="js-kwc-btn-policy-privacy {{ bannerClasses.btnPrivacy }}">Informativa privacy</a>
        {% endif %}
        {% if cookie_url %}
            <a href="{{ cookie_url }}" class="js-kwc-btn-policy-cookie {{ bannerClasses.btnCookie }}">Cookie policy</a>
        {% endif %}
    </div>

</div>